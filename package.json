{
  "name": "test",
  "version": "0.1.0",
  "description": "Test app",
  "main": "server.js",
  "scripts": {
    "dev": "NODE_ENV=development next dev",
    "build": "next build",
    "export": "next build && next export -o dist",
    "start": "PORT=8081 NODE_ENV=production node server.js"
  },
  "license": "ISC",
  "dependencies": {
    "next": "^10.0.5",
    "next-translate": "^1.0.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
